const Tag = ({ tag }) => {

    return ( 

        <div>
            <p>{ tag }</p>

            <button>
                <i className="fi fi-br-cross"></i>
            </button>
        </div>
    )
}


export default Tag